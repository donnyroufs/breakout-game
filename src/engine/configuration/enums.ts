export enum CanvasConfig {
  height = 600,
  width = 800,
  border = "1px solid black",
}

export enum CollisionType {
  canvas,
  entity,
}

export enum CollisionDir {
  top,
  right,
  bottom,
  left,
}
